---
# Sample inventory: dedicated Auth and Proxy instances.
all:
  children:
    teleport_auth:
      hosts:
        auth1.example.com:
          ansible_host: 10.0.1.10
    teleport_proxy:
      hosts:
        proxy1.example.com:
          ansible_host: 203.0.113.20
