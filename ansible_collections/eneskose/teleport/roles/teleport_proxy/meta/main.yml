---
galaxy_info:
  role_name: teleport_proxy
  author: Enes Köse
  description: >-
    Configure and run the Teleport Proxy Service on a dedicated, internet-facing
    instance, joined to an existing Auth Service.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: ["8", "9"]
  galaxy_tags:
    - teleport
    - security
    - pam

dependencies:
  - role: teleport_install
