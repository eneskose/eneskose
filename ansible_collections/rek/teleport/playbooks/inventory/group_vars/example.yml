---
# Environment-specific variables for the 'example' environment.
teleport_auth_cluster_name: "teleport.example.com"
teleport_auth_webauthn_rp_id: "teleport.example.com"

# Proxy -> Auth over the private network (host:3025).
teleport_auth_server: "10.0.1.10:3025"

# CA pin from `tctl status` on the Auth host (strongly recommended).
teleport_ca_pin:
  - "sha256:REPLACE_WITH_REAL_PIN"

# Public entrypoint + ACME contact.
teleport_proxy_public_addr: "teleport.example.com:443"
teleport_proxy_acme_email: "ops@example.com"
