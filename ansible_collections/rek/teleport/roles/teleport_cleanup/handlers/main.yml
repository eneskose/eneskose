---
- name: Reload systemd daemon
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Reset failed Teleport unit
  ansible.builtin.command:
    cmd: "systemctl reset-failed {{ teleport_service_name }}.service"
  changed_when: false
  failed_when: false
