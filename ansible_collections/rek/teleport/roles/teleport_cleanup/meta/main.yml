---
galaxy_info:
  role_name: teleport_cleanup
  author: Enes Köse
  description: >-
    Completely remove Teleport from a host (service, package, repo, binaries,
    config, data, user) and return it to a clean state ready for a fresh install.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: ["8", "9"]
  galaxy_tags:
    - teleport
    - security
    - system
    - cleanup

# Standalone role — deliberately depends on nothing so it never reinstalls.
dependencies: []
