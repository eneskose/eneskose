---
galaxy_info:
  role_name: teleport_install
  author: Enes Köse
  description: >-
    Install Teleport Enterprise (or OSS) and provision the common base layer:
    repository/tarball install, system user, data directory, Enterprise
    license, hardened systemd unit and shared handlers.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: ["8", "9"]
  galaxy_tags:
    - teleport
    - security
    - system
    - pam

# Base role — depends on nothing. The service roles depend on this one.
dependencies: []
